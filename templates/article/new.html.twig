{% extends 'base.html.twig' %}

{% block title %}New Article{% endblock %}

{% block body %}
<div class="container mt-4">
    <div class="card">
        <div class="card-body">
            <h1 class="card-title">Create new Article</h1>

            {{ include('article/_form.html.twig') }}

            <a href="{{ path('app_article_index') }}" class="btn btn-outline-secondary mt-3">
                <i class="fas fa-arrow-left"></i> Back to list
            </a>
        </div>
    </div>
</div>
{% endblock %}